Intentionally empty: this district directory had no CSV in the 2015 source tree.
It must still exist -- data/dataAssemble.R indexes list.dirs() positionally (struc[5], struc[6]),
so a missing directory would shift every later index and change what gets read.
